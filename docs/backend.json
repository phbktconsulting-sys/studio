
{
  "entities": {
    "WorkItem": {
      "title": "WorkItem",
      "type": "object",
      "description": "Represents a work item in the workflow management system.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the WorkItem entity."
        },
        "assignedTo": {
          "type": "string",
          "description": "Reference to User who is assigned to this work item."
        },
        "urgency": {
          "type": "string",
          "enum": ["High", "Medium", "Low"],
          "description": "The urgency level of the work item."
        },
        "status": {
          "type": "string",
          "enum": ["Open", "In Progress", "Closed", "Pending"],
          "description": "The current status of the work item."
        },
        "subject": {
          "type": "string",
          "description": "A brief subject or title for the work item."
        },
        "createdAt": {
          "type": "string",
          "description": "The date and time when the work item was created.",
          "format": "date-time"
        },
        "updatedAt": {
          "type": "string",
          "description": "The date and time when the work item was last updated.",
          "format": "date-time"
        },
        "overview": {
          "type": "string",
          "description": "A detailed description of the work item."
        }
      },
      "required": [
        "id",
        "assignedTo",
        "urgency",
        "status",
        "subject",
        "createdAt",
        "updatedAt"
      ]
    },
    "User": {
      "title": "User",
      "type": "object",
      "description": "Represents a user in the system.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the User entity." },
        "uid": { "type": "string", "description": "Firebase Authentication User ID." },
        "employeeId": { "type": "string", "description": "The user's unique employee ID." },
        "email": { "type": "string", "description": "The user's email address.", "format": "email" },
        "displayName": { "type": "string", "description": "The user's full display name." },
        "firstName": { "type": "string", "description": "The user's first name." },
        "middleName": { "type": "string", "description": "The user's middle name (optional)." },
        "lastName": { "type": "string", "description": "The user's last name." },
        "dob": { "type": "string", "description": "Date of birth.", "format": "date" },
        "mobileNumber": { "type": "string", "description": "User's mobile phone number." },
        "department": { "type": "string", "description": "The department the user belongs to." },
        "jobTitle": { "type": "string", "description": "The user's job title or position." },
        "level": { "type": "string", "description": "The user's level in the organization." },
        "workLocation": { "type": "string", "description": "The user's work location." },
        "company": { "type": "string", "description": "The company the user works for." },
        "aadharNumber": { "type": "string", "description": "The user's Aadhar number." },
        "panNumber": { "type": "string", "description": "The user's PAN number." },
        "role": { "type": "string", "enum": ["Admin", "User"], "description": "The user's role in the system." }
      },
      "required": [
        "id",
        "uid",
        "employeeId",
        "email",
        "displayName",
        "firstName",
        "lastName",
        "dob",
        "mobileNumber",
        "department",
        "jobTitle",
        "level",
        "workLocation",
        "company",
        "aadharNumber",
        "panNumber",
        "role"
      ]
    },
    "Note": {
      "title": "Note",
      "type": "object",
      "description": "Represents a note associated with a work item.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Note entity."
        },
        "workItemId": {
          "type": "string",
          "description": "Reference to the WorkItem this note belongs to."
        },
        "authorId": {
          "type": "string",
          "description": "Reference to the User who created the note."
        },
        "text": {
          "type": "string",
          "description": "The content of the note."
        },
        "createdAt": {
          "type": "string",
          "description": "The date and time when the note was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "workItemId",
        "authorId",
        "text",
        "createdAt"
      ]
    },
     "Counter": {
      "title": "Counter",
      "type": "object",
      "description": "A document to store a counter value for atomic increments.",
      "properties": {
        "count": {
          "type": "number",
          "description": "The current value of the counter."
        }
      },
      "required": ["count"]
    }
  },
  "auth": {
    "providers": [
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "Stores user profiles. Path-based ownership ensures only the user (or an admin) can access their profile. User's role is stored within this document."
    },
    "/work_items/{workitemId}": {
      "schema": { "$ref": "#/entities/WorkItem" },
      "description": "Stores work items. Access is controlled by the 'assignedTo' field."
    },
    "/work_items/{workitemId}/notes/{noteId}": {
      "schema": { "$ref": "#/entities/Note" },
      "description": "Stores notes associated with a specific work item. Access is inherited from the parent work item."
    },
    "/counters/user_employee_id": {
      "schema": { "$ref": "#/entities/Counter" },
      "description": "Stores the counter for generating unique employee IDs for users."
    }
  }
}

    